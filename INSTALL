To install/ compile the two tools,

  make

This will compile the follwing programs: 

1. filterFastq
    The program consist of two modes:
	[ ] Result fastq file will contain all reads that has an ID matching id in <idFile> 
    [-v]  Result fastq file will contain all reads that do not has ID matching id in <idFile>

    Usage: ./filterFastq [-v] -q <fastq file> -i <idFile>




